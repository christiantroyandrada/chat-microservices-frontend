# Example environment file for chat-microservices-frontend
# Copy this file to `.env` and edit values for your environment.
# Do NOT commit secrets for production; use a secure secret manager.

# Frontend / gateway URLs
# URL where the API & websocket gateway (nginx) is available on your host.
# For local development using the provided docker-compose setup, use the nginx port (85).
PUBLIC_API_URL=http://localhost:85

# Socket / realtime endpoint. This should point to the same origin as the gateway
# so the browser can send httpOnly cookies during the Socket.IO handshake.
# Use http(s) here; Socket.IO client will upgrade to ws(s) automatically.
PUBLIC_WS_URL=http://localhost:85

# App metadata used in the UI
PUBLIC_APP_NAME="Chat App"
PUBLIC_APP_VERSION=0.0.1

# Optional: enable verbose client-side logs (true/false)
#PUBLIC_DEBUG=false
