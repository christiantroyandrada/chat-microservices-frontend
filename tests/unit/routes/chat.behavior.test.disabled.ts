// Placeholder file (disabled). The active behavior tests live in:
// tests/unit/routes/chat.behavior.svelte.test.ts and run under the client project.

// Intentionally named *.test.disabled.ts so it's not picked up by Vitest's server include glob.

export {};
