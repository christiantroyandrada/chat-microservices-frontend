import { describe, it } from 'vitest';

// Minimal placeholder test so the server test project doesn't error on an empty file.
describe('chat.behavior (placeholder)', () => {
	it('placeholder', () => {
		// no-op server-side placeholder for behavior tests
	});
});
