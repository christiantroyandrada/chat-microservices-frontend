name: ci - master
on:
  # Run this workflow when changes are pushed to the main branch
  # (post-PR merge / direct pushes to main)
  push:
    branches: [main, master]

jobs:
  build-and-sonar:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        # Pinned to specific commit SHA for supply-chain security
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5

      - name: Setup Node.js
        # Pinned to specific commit SHA for supply-chain security
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020
        with:
          node-version: '22'

      - name: Install pnpm
        run: npm install -g pnpm

      - name: Install frontend deps
        working-directory: ./
        run: pnpm install

      - name: npm/pnpm audit (dependency vulnerability check)
        working-directory: ./
        run: |
          # Try pnpm audit first; fall back to npm audit for compatibility
          pnpm audit --audit-level=moderate || npm audit --audit-level=moderate || true

      - name: Lint (Prettier + ESLint)
        working-directory: ./
        run: pnpm run lint

      - name: TypeScript type check
        working-directory: ./
        run: pnpm exec tsc --noEmit

      - name: Svelte check
        working-directory: ./
        run: pnpm run check

      - name: Build FE (prepare + build)
        working-directory: ./
        run: |
          pnpm run prepare || true
          pnpm run build

      - name: Ensure Playwright browsers are installed
        working-directory: ./
        run: pnpm exec playwright install

      - name: Ensure coverage temp directory exists
        working-directory: ./
        run: mkdir -p coverage/.tmp

      - name: Run unit tests and generate coverage
        working-directory: ./
        run: pnpm run coverage:unit
        timeout-minutes: 15

      - name: Check Sonar secrets/vars
        working-directory: ./
        run: |
          if [ -z "$SONAR_TOKEN" ]; then
            echo "::error::SONAR_TOKEN is not set. Add it under Settings → Secrets and variables → Actions.";
            exit 1;
          fi
          if [ -z "$SONAR_ORGANIZATION_SECRET" ] && [ -z "$SONAR_ORGANIZATION_VAR" ]; then
            echo "::error::SONAR_ORGANIZATION is not set as a secret or variable. Add it under Settings → Secrets and variables → Actions.";
            exit 1;
          fi
          echo "Sonar secrets/vars present. Proceeding..."
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
          SONAR_ORGANIZATION_SECRET: ${{ secrets.SONAR_ORGANIZATION }}
          SONAR_ORGANIZATION_VAR: ${{ vars.SONAR_ORGANIZATION }}

      - name: SonarCloud Scan
        # Pinned to specific commit SHA for supply-chain security
        uses: SonarSource/sonarqube-scan-action@fd88b7d7ccbaefd23d8f36f73b59db7a3d246602
        with:
          args: >
            -Dsonar.organization=${{ secrets.SONAR_ORGANIZATION }}
            -Dsonar.projectKey=${{ secrets.SONAR_PROJECT_KEY }}
            -Dsonar.javascript.lcov.reportPaths=coverage/lcov.info
            -Dsonar.exclusions=tests/**,test/**,__mocks__/**,mocks/**
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
          SONAR_ORGANIZATION: ${{ secrets.SONAR_ORGANIZATION }}

  # Deploy to VPS (only runs on main branch after build and tests pass)
  deploy:
    name: Deploy Frontend to VPS
    needs: build-and-sonar
    if: github.ref == 'refs/heads/master'
    runs-on: ubuntu-latest
    environment: production
    concurrency:
      group: production-frontend-deployment
      cancel-in-progress: false

    steps:
      - name: Deploy Frontend via SSH
        uses: appleboy/ssh-action@029f5b4aeeeb58fdfe1410a5d17f967dacf36262
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_PRIVATE_KEY }}
          port: ${{ secrets.VPS_PORT || '22' }}
          command_timeout: 20m
          script: |
            set -euo pipefail
            export DEPLOY_PATH="${{ secrets.DEPLOY_PATH || '/opt/chat-app' }}"

            echo "🚀 Starting frontend deployment..."

            # Ensure frontend repo directory exists; clone if missing
            if [ -d "$DEPLOY_PATH/chat-microservices-frontend" ]; then
              echo "➡️ Found existing $DEPLOY_PATH/chat-microservices-frontend — updating"
              cd "$DEPLOY_PATH/chat-microservices-frontend"
              if git rev-parse --is-inside-work-tree >/dev/null 2>&1; then
                git fetch origin --prune || true
                git reset --hard origin/main || true
              else
                echo "⚠️ Directory exists but is not a git repo — removing and re-cloning"
                cd "$DEPLOY_PATH"
                rm -rf chat-microservices-frontend
              fi
            fi

            if [ ! -d "$DEPLOY_PATH/chat-microservices-frontend" ]; then
              echo "➡️ Directory not found — attempting to clone frontend repository"
              mkdir -p "$DEPLOY_PATH"
              cd "$DEPLOY_PATH"
              REPO_URL="https://github.com/${{ github.repository }}.git"
              if git ls-remote --exit-code "$REPO_URL" >/dev/null 2>&1; then
                git clone --depth 1 --branch main "$REPO_URL" chat-microservices-frontend || {
                  echo "⚠️ git clone failed for frontend repo; creating empty folder"
                  mkdir -p chat-microservices-frontend
                }
                cd chat-microservices-frontend || true
              else
                echo "⚠️ Cannot access $REPO_URL (permissions or network). Creating empty directory and continuing"
                mkdir -p chat-microservices-frontend
                cd chat-microservices-frontend
              fi
            fi

            # Ensure main microservices directory exists (where docker-compose files live)
            if [ ! -d "$DEPLOY_PATH/chat-microservices" ]; then
              echo "⚠️ $DEPLOY_PATH/chat-microservices not found — creating and continuing"
              mkdir -p "$DEPLOY_PATH/chat-microservices"
            fi

            cd "$DEPLOY_PATH/chat-microservices"

            echo "🏗️  Building frontend service (if defined in compose files)..."

            # Determine compose files to use
            COMPOSE_ARGS=()
            if [ -f docker-compose.yml ] && [ -f docker-compose.prod.yml ]; then
              COMPOSE_ARGS+=( -f docker-compose.yml -f docker-compose.prod.yml )
            elif [ -f docker-compose.yml ]; then
              COMPOSE_ARGS+=( -f docker-compose.yml )
            elif [ -f docker-compose.prod.yml ]; then
              COMPOSE_ARGS+=( -f docker-compose.prod.yml )
            else
              echo "⚠️ No docker-compose files found in $PWD — skipping docker compose steps"
            fi

            # If compose files are present, try to build and bring up the frontend service
            if [ ${#COMPOSE_ARGS[@]} -gt 0 ]; then
              # Check if 'frontend' service is defined
              if docker compose "${COMPOSE_ARGS[@]}" config --services 2>/dev/null | grep -q '^frontend$'; then
                docker compose "${COMPOSE_ARGS[@]}" build frontend || echo "⚠️ Build step failed (continuing)"
                docker compose "${COMPOSE_ARGS[@]}" up -d frontend || echo "⚠️ Up step failed (continuing)"

                echo "⏳ Waiting for frontend to be healthy..."
                sleep 15

                if docker compose "${COMPOSE_ARGS[@]}" ps frontend | grep -q "healthy"; then
                  echo "✅ Frontend is healthy"
                  echo "🔄 Restarting nginx (if defined)"
                  if docker compose "${COMPOSE_ARGS[@]}" config --services 2>/dev/null | grep -q '^nginx$'; then
                    docker compose restart nginx || true
                    sleep 5
                  fi

                  if curl -f http://localhost:85/ > /dev/null 2>&1; then
                    echo "✅ Frontend accessible through nginx"
                    echo "🎉 Frontend deployment successful!"
                  else
                    echo "⚠️ Frontend is running but not accessible through nginx"
                    docker compose logs nginx --tail=20 || true
                  fi
                else
                  echo "❌ Frontend deployment failed or service not healthy"
                  docker compose "${COMPOSE_ARGS[@]}" ps frontend || true
                  docker compose "${COMPOSE_ARGS[@]}" logs frontend --tail=50 || true
                  exit 1
                fi
              else
                echo "⚠️ 'frontend' service not defined in compose files — skipping build/up steps"
              fi
            fi
